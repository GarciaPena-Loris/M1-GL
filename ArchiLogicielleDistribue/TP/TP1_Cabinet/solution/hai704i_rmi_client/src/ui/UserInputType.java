package ui;

/**
 * The UserInputType enum enumerates the types of user inputs supported by the
 * user input processors. It is used for selecting the appropriate processor.
 */
public enum UserInputType {
	STRING,
	STRICTLY_POSITIVE_INTEGER
}
